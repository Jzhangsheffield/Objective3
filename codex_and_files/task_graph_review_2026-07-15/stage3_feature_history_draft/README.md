# Stage 3 feature-history draft

- `stage3_task_graph_latest.json`: nodes 26–36 with global mandatory/possible/optional history sets.
- `stage3_feature_history_standalone.html`: standalone execution graph and local history matrix.
- `stage3-feature-history-graph.html`: editable visualization fragment.

Immediate pairs are `[26, 27]` and `[34, 35]`. Node 32 is a normal mandatory predecessor of nodes 33 and 34, so other shutdown actions may be inserted between them.
