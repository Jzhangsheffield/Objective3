# Experiment outputs

All newly generated artifacts are written here. `upstream/` contains sequence-disjoint fold×seed backbones and freshly extracted feature caches; `history_models/` contains the five M2/A1-Legacy/A3-DualPos grids; `summary/` contains final normal/fault/all comparisons. Existing completed jobs are skipped by default.
